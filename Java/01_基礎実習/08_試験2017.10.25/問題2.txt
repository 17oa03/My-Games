■問題2

▼(1)○
Javaで多重継承は禁止されています。
理由は、両方のスーパークラスから
同じ名前の異なる内容のメソッドを継承して衝突してしまうからです。

▼(2)×
抽象クラスの制約は、インスタンス化が禁止されていることです。
抽象メソッドのみ持つクラスはインターフェースです、
また、インターフェースは基本的にフィールドを持ちません。

▼(3)○
実装するクラス側で、
全ての抽象メソッドの処理内容が確定しなければインスタンス化ができません。
メソッドをオーバーライドして内容を確定させることを実装すると表現します。